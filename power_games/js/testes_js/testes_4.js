let limite = 100;
let saldo_atual = 1000;

let resultado = (saldo_atual < limite) ? "Necessito depositar $$$" : "Não necessita Depósito";

alert(resultado)

/* 

Sintaxe:
let variável = (condição) ? //instrução se true : //instrução se false

*/